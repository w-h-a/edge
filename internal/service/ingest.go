package service

// IngestManager captures context from producers (bees, file changes)
// and routes to the store with appropriate merge strategy.
type IngestManager struct {
	// ports injected at construction
}
